info:
  name: Get Books by Author
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/authors/5/books"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
