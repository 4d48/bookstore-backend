info:
  name: Get Books by Author
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/authors/2/books"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
