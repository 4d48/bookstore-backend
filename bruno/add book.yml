info:
  name: add book
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/books"
  body:
    type: json
    data: |-
      {
        "title": "1984"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
