info:
  name: add
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/api/books"
  body:
    type: json
    data: |-
      {
        "title": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
