info:
  name: get author books
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/authors/2/books"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
