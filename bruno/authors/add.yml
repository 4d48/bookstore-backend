info:
  name: add
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/authors"
  body:
    type: json
    data: |-
      {
        "name": "George Orwell",
        "books": [2]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
