info:
  name: Delete Book
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/books/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
