info:
  name: Update Book
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/books/2"
  body:
    type: json
    data: |-
      {
        "title": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
